name: grpc
on:
  push:
    branches:
      - master
      - stable
  pull_request:
    branches:
      - master
      - stable
jobs:
  grpc_test:
    name: gRPC plugin (Go ${{ matrix.go }}, PHP ${{ matrix.php }}, OS ${{matrix.os}})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      matrix:
        php: ["8.5"]
        go: [stable]
        os: ["ubuntu-latest"]
    steps:
      - name: Set up Go ${{ matrix.go }}
        uses: actions/setup-go@v7 # action page: <https://github.com/actions/setup-go>
        with:
          go-version: ${{ matrix.go }}
      - name: Set up PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@v2 # action page: <https://github.com/shivammathur/setup-php>
        with:
          php-version: ${{ matrix.php }}
          extensions: sockets
      - name: Check out code
        uses: actions/checkout@v7
      - name: Get Composer Cache Directory
        id: composer-cache
        run: |
          cd tests/php_test_files
          echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT
      - name: Init Composer Cache # Docs: <https://git.io/JfAKn#php---composer>
        uses: actions/cache@v5
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ matrix.php }}-${{ hashFiles('**/composer.json') }}
          restore-keys: ${{ runner.os }}-composer-
      - name: Install Composer dependencies
        run: cd tests/php_test_files && composer update --prefer-dist --no-progress --ansi
      - name: Init Go modules Cache # Docs: <https://git.io/JfAKn#go---modules>
        uses: actions/cache@v5
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: ${{ runner.os }}-go-
      - name: Install Go dependencies
        run: go mod download
      - name: Create folders
        run: |
          mkdir ./tests/coverage-ci
      - name: Install protoc
        uses: arduino/setup-protoc@v3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - name: Run golang root module tests
        run: |
          go test -timeout 20m -v -race -cover -tags=debug -coverpkg=./... -coverprofile=./tests/coverage-ci/grpc_root.out -covermode=atomic ./...
      - name: Run ee tests with coverage
        run: |
          cd tests

          sudo apt update
          sudo apt install -y libnss3-tools
          curl -JLO "https://dl.filippo.io/mkcert/latest?for=linux/amd64"
          chmod +x mkcert-v*-linux-amd64
          sudo cp mkcert-v*-linux-amd64 /usr/local/bin/mkcert
          mkcert -install
          mkcert localhost 127.0.0.1 ::1
          mkcert -client localhost 127.0.0.1 ::1
          mkdir test-certs
          cp -r localhost+2-client-key.pem localhost+2-client.pem localhost+2-key.pem localhost+2.pem test-certs/
          cp -r "$(mkcert -CAROOT)/rootCA.pem" test-certs/

          go test -timeout 20m -v -race -cover -tags=debug -coverpkg=github.com/roadrunner-server/grpc/v6/... -coverprofile=./coverage-ci/grpc.out -covermode=atomic ./...
      - name: Archive code coverage results
        uses: actions/upload-artifact@v7
        with:
          name: coverage
          path: ./tests/coverage-ci
  codecov:
    name: Upload codecov
    runs-on: ubuntu-latest
    needs:
      - grpc_test
    timeout-minutes: 60
    steps:
      - name: Check out code
        uses: actions/checkout@v7
      - name: Download code coverage results
        uses: actions/download-artifact@v8
        with:
          name: coverage
          path: coverage
      - run: |
          echo 'mode: atomic' > summary.txt
          tail -q -n +2 coverage/*.out >> summary.txt
          awk '
            NR == 1 { print; next }
            /^github\.com\/roadrunner-server\/grpc\/v6\// {
              sub(/^github\.com\/roadrunner-server\/grpc\/v6\//, "", $0)
              print
              next
            }
            /^github\.com\/roadrunner-server\/grpc\/protoc_plugins\/v6\// {
              sub(/^github\.com\/roadrunner-server\/grpc\/protoc_plugins\/v6\//, "protoc_plugins/", $0)
              print
            }
          ' summary.txt > summary.filtered.txt
          mv summary.filtered.txt summary.txt
          # a profile that maps to no plugin source uploads fine and reports 0%
          blocks=$(($(wc -l < summary.txt) - 1))
          if [ "$blocks" -lt 10 ]; then
            echo "::error::coverage summary holds $blocks blocks, the profile does not map to plugin sources"
            exit 1
          fi
      - name: upload to codecov
        uses: codecov/codecov-action@v7 # Docs: <https://github.com/codecov/codecov-action>
        with:
          files: summary.txt
          fail_ci_if_error: false
